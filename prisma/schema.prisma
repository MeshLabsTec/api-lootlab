generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String    @id @default(uuid())
  name      String?
  email     String?   @unique
  password  String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  role      Role?     @default(USER)
  posts     Post[]    @relation("UserPosts")
  comments  Comment[] @relation("UserComments")
  likes     Like[]    @relation("UserLikes")
}

model Post {
  id              String    @id @default(uuid())
  title           String    @db.VarChar(255)
  category        String    @db.VarChar(255)
  marketLink      String?   @db.VarChar(255)
  score           Int?      @default(0)
  investment      String?   @db.VarChar(255)
  token           String?   @db.VarChar(255)
  network         String?   @db.VarChar(255)
  commentAuthor   String?   @db.Text
  slug            String    @unique
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  authorId        String?   @db.VarChar(255)
  genres          String[] // Array de strings para armazenar os gêneros diretamente
  launchInfo      Json? // Armazena informações de lançamento como um JSON
  projectFeatures Json? // Armazena recursos do projeto como um JSON
  links           Json? // Armazena links como um JSON
  partnerships    Json? // Armazena parcerias como um JSON
  images          String[] // URLs de imagens armazenadas como array de strings
  comments        Comment[] @relation("PostComments")
  likes           Like[]    @relation("PostLikes")
  author          User?     @relation("UserPosts", fields: [authorId], references: [id])
}

model Comment {
  id        String    @id @default(uuid())
  content   String?
  createdAt DateTime  @default(now())
  deletedAt DateTime?
  post      Post?     @relation("PostComments", fields: [postId], references: [id], onDelete: Cascade)
  postId    String?
  user      User?     @relation("UserComments", fields: [userId], references: [id])
  userId    String?
}

model Like {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  post      Post?    @relation("PostLikes", fields: [postId], references: [id], onDelete: Cascade)
  postId    String?
  user      User?    @relation("UserLikes", fields: [userId], references: [id])
  userId    String?
}

enum Role {
  ADMIN
  WRITER
  USER
}

enum PostStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}
